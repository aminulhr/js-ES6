const words = ['spray', 'elite', 'exuberant', 'destruction', 'present'];

const result = words.filter((word) => word.length > 6);

console.log(result);

const words2 = ['spray', 'elite', 'exuberant', 'destruction', 'present'];

const result2 = words.filter((word2) => word2.length > 6);

console.log(result2);